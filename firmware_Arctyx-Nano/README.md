# Firmware for interfacing with the FPGA

## ----WIP----

I am writing verilog in visual studio code and building via the default ICE40-UP5K config available in APIO.

The upload process is very sketchy(WIP). You first upload the bitstream file to the rp2350 in thonny and then upload it to the FPGA via a custom script.